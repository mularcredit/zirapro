app = "mpesa"
primary_region = "cdg"

[http_service]
  # 👇 Your Node app listens on 3001, so Fly should talk to 3001 internally
  internal_port = 3001

  force_https = true
  auto_start_machines = true
  auto_stop_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024

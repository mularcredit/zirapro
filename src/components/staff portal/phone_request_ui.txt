// Phone Number Change Request Modal and UI Components
// Add this modal component near the end of the Bio.tsx file, before the final closing tags

{/* Phone Number Change Request Modal */}
{showPhoneRequestModal && (
  <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4">
    <motion.div
      initial={{ opacity: 0, scale: 0.95 }}
      animate={{ opacity: 1, scale: 1 }}
      className="bg-white rounded-lg shadow-xl max-w-md w-full p-6"
    >
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-semibold text-gray-900">Request Phone Number Change</h3>
        <button
          onClick={() => {
            setShowPhoneRequestModal(false);
            setRequestedPhone('');
            setPhoneChangeReason('');
          }}
          className="text-gray-400 hover:text-gray-600"
        >
          <X className="h-5 w-5" />
        </button>
      </div>

      <div className="space-y-4">
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-1">
            Current Phone Number
          </label>
          <p className="text-sm text-gray-900 bg-gray-50 px-3 py-2 rounded-lg">
            {employee['Mobile Number'] || 'Not set'}
          </p>
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-1">
            New Phone Number <span className="text-red-500">*</span>
          </label>
          <input
            type="tel"
            value={requestedPhone}
            onChange={(e) => setRequestedPhone(e.target.value)}
            placeholder="e.g., +254712345678"
            className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-green-500"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-1">
            Reason for Change (Optional)
          </label>
          <textarea
            value={phoneChangeReason}
            onChange={(e) => setPhoneChangeReason(e.target.value)}
            rows={3}
            placeholder="Please provide a reason for this change..."
            className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-green-500"
          />
        </div>

        <div className="bg-blue-50 border border-blue-200 rounded-lg p-3">
          <div className="flex items-start">
            <AlertCircle className="h-4 w-4 text-blue-600 mt-0.5 mr-2 flex-shrink-0" />
            <p className="text-xs text-blue-800">
              Your request will be reviewed by an administrator. You will be notified once it's approved or rejected.
            </p>
          </div>
        </div>
      </div>

      <div className="flex space-x-3 mt-6">
        <button
          onClick={() => {
            setShowPhoneRequestModal(false);
            setRequestedPhone('');
            setPhoneChangeReason('');
          }}
          disabled={isSubmittingRequest}
          className="flex-1 px-4 py-2 border border-gray-300 text-gray-700 text-sm font-medium rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50"
        >
          Cancel
        </button>
        <button
          onClick={submitPhoneChangeRequest}
          disabled={isSubmittingRequest || !requestedPhone.trim()}
          className="flex-1 px-4 py-2 bg-green-600 text-white text-sm font-medium rounded-lg hover:bg-green-700 transition-colors disabled:opacity-50 flex items-center justify-center"
        >
          {isSubmittingRequest ? (
            <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-white"></div>
          ) : (
            'Submit Request'
          )}
        </button>
      </div>
    </motion.div>
  </div>
)}

// Replace the Primary Mobile Number field in the Contact Information section with this:
// Find line ~1332 and replace the FormField for "Primary Mobile Number" with:

<div>
  <label className="block font-medium text-gray-700 mb-1">
    Primary Mobile Number <span className="text-red-500">*</span>
  </label>
  <div className="flex items-center space-x-2">
    <input
      type="tel"
      value={employee['Mobile Number'] || ''}
      className="flex-1 h-11 bg-gray-50 border-gray-200 cursor-not-allowed border rounded-lg px-4 py-2 text-gray-900"
      readOnly
      disabled
    />
    {userRole === 'employee' && (
      <button
        onClick={() => setShowPhoneRequestModal(true)}
        className="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors flex items-center whitespace-nowrap"
      >
        <Bell className="h-4 w-4 mr-2" />
        Request Change
      </button>
    )}
  </div>
  {pendingPhoneRequest && (
    <div className="mt-2 p-3 bg-yellow-50 border border-yellow-200 rounded-lg">
      <div className="flex items-start">
        <Clock className="h-4 w-4 text-yellow-600 mt-0.5 mr-2 flex-shrink-0" />
        <div className="flex-1">
          <p className="text-xs font-medium text-yellow-800">Pending Request</p>
          <p className="text-xs text-yellow-700 mt-1">
            You have requested to change your number to{' '}
            <span className="font-medium">{pendingPhoneRequest.requested_phone}</span>
          </p>
          <p className="text-xs text-yellow-600 mt-1">
            Requested on {new Date(pendingPhoneRequest.requested_at).toLocaleDateString()}
          </p>
        </div>
      </div>
    </div>
  )}
  {errors['Mobile Number'] && (
    <p className="mt-1 text-xs text-red-500">{errors['Mobile Number']}</p>
  )}
</div>
